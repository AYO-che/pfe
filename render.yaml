services:
  - type: web
    name: chrysalise-user
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    rootDir: user
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
